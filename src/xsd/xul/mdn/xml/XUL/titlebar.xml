<content type="application/x.deki0805+xml" title="titlebar"><body xmlns:eval="http://mindtouch.com/2007/dekiscript"><div class="noinclude"><span class="script">template.XULRefElem()</span></div><p>Boxes created with the titlebar element behave just like a normal window titlebar: when the element is clicked and dragged, the window moves with it. This element can't be used to change the platform-native titlebar of normal windows; instead, it's used to create custom titlebars for chromeless windows. If the titlebar is in a panel element, dragging it will move the panel.</p><p>Elements inside the titlebar usually don't receive any mouse events, so e.g. buttons inside it can't be clicked and textboxes can't be focused with the mouse. If you don't want this behavior, you can override it by setting <code>allowevents="true"</code> on the titlebar element.</p><p>The titlebar will send a command event after the move is complete.</p><h3 name="Examples">Examples</h3><p>This XUL file will create a HUD-style window that can be moved with the mouse. It will close if the mouse button is released.</p><pre>
&lt;?xml version="1.0"?&gt;
&lt;window title="Movable HUD Window"
        xmlns="http://www.mozilla.org/keymaster/gatekeeper/there.is.only.xul"
        width="300" height="200"
        style="background: transparent; -moz-appearance: none;"&gt;
  &lt;titlebar flex="1" oncommand="close()"
            style="background: rgba(30, 30, 30, 0.9);
                   -moz-border-radius: 10px;
                   -moz-box-shadow: 0 1px 8px rgba(0, 0, 0, 0.8);
                   margin: 8px 12px 16px;"/&gt;
&lt;/window&gt;
</pre><p>It can be opened from the error console like this: <code>open("<a rel="freelink" class="external" href="file:///Users/markus/Sites/hudwindow.xul">file:///Users/markus/Sites/hudwindow.xul</a>", "", "chrome=1, titlebar=0")</code></p><h3 name="Attributes">Attributes</h3><p><span class="script">template.XULAttrInheritedWide()</span></p><p><span class="script">template.Note("The &lt;code&gt;allowevents&lt;/code&gt; attribute did not work for title bars prior to Firefox 3.")</span></p><h3 name="Properties">Properties</h3><p><span class="script">template.XULPropInheritedWide()</span></p><h3 name="Methods">Methods</h3><p><span class="script">template.XULMethInheritedWide()</span></p><h3 name="Related">Related</h3><p>TBD</p><p><span class="script">wiki.languages( { "ja": "ja/XUL/titlebar", "pl": "pl/XUL/titlebar" } )</span></p></body></content>