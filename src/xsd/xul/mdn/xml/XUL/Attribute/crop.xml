<content type="application/x.deki0805+xml" title="crop"><body xmlns:eval="http://mindtouch.com/2007/dekiscript"><div class="noinclude"><span class="script">template.XULRefAttr()</span></div><dl><dt><span class="script">template.XULAttr("crop")</span></dt><dd>Type: <em>one of the values below</em></dd><dd>If the label of the element is too big to fit in its given space, the text will be cropped on the side specified by the <code>crop</code> attribute. An ellipsis will be used in place of the cropped text. If the box direction is reversed, the cropping is reversed.</dd><dd><dl><dt><code>start</code></dt><dd>The text will be cropped on its left side.</dd><dt><code>end</code></dt><dd>The text will be cropped on its right side.</dd><dt><code>left</code></dt><dd><span class="script">template.Deprecated_inline()</span> The text will be cropped on its left side.</dd><dt><code>right</code></dt><dd><span class="script">template.Deprecated_inline()</span> The text will be cropped on its right side.</dd><dt><code>center</code></dt><dd>The text will be cropped in the middle, showing both the start and end of the text normally.</dd><dt><code>none</code></dt><dd>The text will be not be cropped using an ellipsis. However, the text will simply be cut off if it is too large. The side depends on the CSS text alignment.</dd></dl></dd><dd>Depending on the platform and theme being used, some elements will have a set maximum width so they will be always appear cropped. If you wish to use the value <code>none</code> and the displayed text is larger than this maximum width, you may be able to use the max-width CSS property (or the maxwidth attribute) to override this size. For example, for a menuitem in a menu you can add the following CSS rule when you want to use the value <code>none</code>:</dd><dd><pre class="eval">
menupopup &gt; menuitem, menupopup &gt; menu { max-width: none; }
</pre></dd>
</dl><div class="noinclude">
<p><span class="script"> wiki.languages( { "fr": "fr/XUL/Attributs/crop", "ja": "ja/XUL/Attribute/crop", "pl": "pl/XUL/Atrybut/crop" } ) </span></p>
</div></body></content>