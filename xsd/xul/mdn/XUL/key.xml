<content type="application/x.deki0805+xml" title="key"><body xmlns:eval="http://mindtouch.com/2007/dekiscript">
<div class="noinclude"><span class="script">template.XULRefElem()</span></div>
<p>The <code>key</code> element defines a window-global keyboard shortcut and must be placed inside a <span class="script">template.XULElem("keyset")</span> element.</p>
<p>When a key matching the attributes on the <code>key</code> element is pressed, the command will be fired on the <code>key</code> element. The key pressed must match the <span class="script">template.PrefAnch("a-", "key")</span> attribute (or <span class="script">template.PrefAnch("a-", "keycode")</span> attribute) as well as the <span class="script">template.PrefAnch("a-", "modifiers")</span> attribute in order for the <code>key</code> element to be activated.</p>
<p>For the shortcut defined using the <code>key</code> element to work. you must specify a <span class="script">template.PrefAnch("a-", "command")</span> attribute (or an <span class="script">template.PrefAnch("a-", "oncommand")</span> handler) on the <code>key</code> element.</p>
<p>To display the shortcut defined with the <code>key</code> element in the UI, you can use &lt;<span class="script">template.XULElem("menuitem")</span><code> key="<em>key element's id</em>" command="<em>command id</em>" .../&gt;</code>.</p>
<p>In order to use (non-default) key commands within specific elements, you will need to listen for key <a title="en/DOM/event" href="/en/DOM/event">events</a>.</p>
<p>More information is available in the <a title="en/XUL_Tutorial/Keyboard_Shortcuts" href="/en/XUL_Tutorial/Keyboard_Shortcuts">XUL tutorial</a>.</p>
<dl> <dt>Attributes</dt> <dd><span class="script">template.PrefAnch("a-", "command")</span>, <span class="script">template.PrefAnch("a-", "disabled")</span>, <span class="script">template.PrefAnch("a-", "key")</span>, <span class="script">template.PrefAnch("a-", "keycode")</span>, <span class="script">template.PrefAnch("a-", "keytext")</span>, <span class="script">template.PrefAnch("a-", "modifiers")</span>, <span class="script">template.PrefAnch("a-", "oncommand")</span>, <span class="script">template.PrefAnch("a-", "phase")</span></dd>
</dl>
<h3 name="Examples">Examples</h3>
<p>(example needed)</p>
<h3 name="Attributes">Attributes</h3>
<p><span class="script">template.XULAttrInherited()</span> <span class="script">template.XULAttrInc("command")</span> <span class="script">template.XULAttrInc("disabled")</span> <span class="script">template.XULAttrInc("key")</span> <span class="script">template.XULAttrInc("keycode")</span> <span class="script">template.XULAttrInc("keytext")</span> <span class="script">template.XULAttrInc("modifiers")</span> <span class="script">template.XULAttrInc("oncommand")</span> <span class="script">template.XULAttrInc("phase")</span></p>
<h3 name="Properties">Properties</h3>
<p><span class="script">template.XULPropInheritedWide()</span></p>
<h3 name="Methods">Methods</h3>
<p><span class="script">template.XULMethInheritedWide()</span></p>
<h3>Details on key, keycode, and modifiers attributes</h3>
<p>For example, consider the following key:</p>
<pre class="eval">
&lt;key key="r" modifiers="shift"/&gt;
</pre>
<p>This key will only match when the Shift key is pressed as well as the R key, and no other keys. For instance, if the Shift, Control and R keys are all pressed, the key will not match.</p>
<p>To indiciate that a modifier key may optionally be pressed, place the word 'any' after listing the optional modifier key. For example:</p>
<pre class="eval">
&lt;key key="r" modifiers="shift any control"/&gt;
</pre>
<p>In this example, the shift key may or may not be pressed, while the control key must be pressed. This allows keys to match more loosely for modifier keys that aren't relevant, yet still allows specific modifiers to be required.</p>
<p>If the modifiers attribute is not specified, then no modifiers may be pressed for the key to match.</p>
<p>If neither the key or keycode attribute are used, the key element will handle all key events. However, if one of the attributes is set to an empty string, the element doesn't handle any key events. For example:</p>
<pre class="eval">
&lt;!-- This element handles all key events --&gt;
&lt;key/&gt;

&lt;!-- These elements don't handle any key events --&gt;
&lt;key key="" modifiers="control"/&gt;
&lt;key keycode="" modifiers="control"/&gt;
</pre>
<h3 name="Related">Related</h3>
<p>TBD</p>
<p><span class="script"> wiki.languages( { "ja": "ja/XUL/key", "pl": "pl/XUL/key" } ) </span></p></body></content>