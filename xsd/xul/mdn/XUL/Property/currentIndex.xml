<content type="application/x.deki0805+xml" title="currentIndex"><body xmlns:eval="http://mindtouch.com/2007/dekiscript">
<div class="noinclude"><span class="script">XULRefProp()</span></div>
<dl> <dt><span class="script">XULProp("currentIndex")</span></dt> <dd>Type: <em>integer</em></dd> <dd> <p>Set to the row index of the tree caret in the <span class="script">XULElem("tree")</span>. For trees with focus, the caret's position is indicated by the focus ring, but unfocused trees won't show  a focus ring, naturally. For unfocused trees, the (undrawn) caret's position can still be obtained by this property. If the caret isn't present for any row (for example, because the tree has never been focused), the value will be <code>-1</code>.</p> <p><strong>You cannot rely on this property to change or determine a tree selection</strong>, except for trees with <code><span class="script">XULAttr("seltype")</span>="single"</code>. (All trees have <code>seltype="multiple"</code> by default.) To reliably change or determine a selection, instead use the <span class="script">interface("nsITreeSelection")</span> interface methods available via <code>tree.view.selection</code>.</p> </dd>
</dl>
<div class="noinclude">
<h2 name="Example">Example</h2>
<pre>
// One way of retrieving the text of a cell.
&lt;script language ="javascript"&gt;
function treeRowClicked(){
    var tree = document.getElementById("my-tree");
    var selection = tree.view.selection;
    var cellText = tree.view.getCellText(tree.currentIndex, tree.columns.getColumnAt(0));
    alert(cellText);
}
&lt;/script&gt;

&lt;tree id="my-tree" seltype="single" onselect="treeRowClicked()"&gt;
  &lt;treecols&gt;
    &lt;treecol label="Title" flex="1"/&gt;&lt;treecol label="URL" flex="1"/&gt;
  &lt;/treecols&gt;
  &lt;treechildren&gt;
    &lt;treeitem&gt;
      &lt;treerow&gt;
        &lt;treecell label="joe@somewhere.com"/&gt;
        &lt;treecell label="Top secret plans"/&gt;
      &lt;/treerow&gt;
    &lt;/treeitem&gt;
    &lt;treeitem&gt;
      &lt;treerow&gt;
        &lt;treecell label="mel@whereever.com"/&gt;
        &lt;treecell label="Let's do lunch"/&gt;
      &lt;/treerow&gt;
    &lt;/treeitem&gt;
  &lt;/treechildren&gt;
&lt;/tree&gt;
</pre>
<h2 name="See_also">See also</h2>
<ul> <li><span class="script">XULProp("listbox.currentIndex")</span></li>
</ul>
</div>
<div class="noinclude">
<p><span class="script"> wiki.languages( { "ja": "ja/XUL/Property/currentIndex", "pl": "pl/XUL/W\u0142asno\u015b\u0107/currentIndex" } ) </span></p>
</div></body></content>