<content type="application/x.deki0805+xml" title="button.type"><body xmlns:eval="http://mindtouch.com/2007/dekiscript">
<div class="noinclude"><span class="script">template.XULRefAttr()</span></div>
<dl> <dt><span class="script">template.XULAttr("button.type", "type")</span></dt> <dd>Type: <em>one of the values below</em></dd> <dd>The type of button. If this attribute is not present, a normal button is created. Leave the attribute out for a normal button.</dd> <dd> <ul> <li><code>checkbox</code>: This type of button can be in two states. The user can click the button to switch between the states. This is not the same as a checkbox because it looks like a button.</li> <li><code>menu</code>: Set the <code>type</code> attribute to the value <code>menu</code> to create a button with a menu popup. Place a <span class="script">template.XULElem("menupopup")</span> element inside the button in this case. The user may click anywhere on the button to open and close the menu.</li> <li><code>menu-button</code>: You can also use the value <code>menu-button</code> to create a button with a menu. Unlike the <code>menu</code> type, this type requires the user to press the arrow to open the menu, but a different command may be invoked when the main part of the button is pressed.</li> <li><code>radio</code>: The button acts like a radio button. Only one button in the group can be on a once.</li> <li><code>repeat</code>: This button will fire its command event repeatedly while the mouse button is held down. <span class="script">template.Fx_minversion_inline(3)</span></li> </ul> </dd>
</dl>
<div class="noinclude">
<p><span class="script"> wiki.languages( { "ja": "ja/XUL/Attribute/button.type", "pl": "pl/XUL/Atrybut/button.type" } ) </span></p>
</div></body></content>