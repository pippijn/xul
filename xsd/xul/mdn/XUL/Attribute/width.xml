<content type="application/x.deki0805+xml" title="width"><body xmlns:eval="http://mindtouch.com/2007/dekiscript">
<div class="noinclude"><span class="script">template.XULRefAttr()</span></div>
<dl><dt> <span class="script">template.XULAttr("width")</span> </dt><dd> Type: <em>string</em> (representing an integer) </dd><dd> The preferred width of the element. The value should not include a unit as all values are in pixels. The actual displayed width may be different if the element or its contents have a minimum or maximum width, or the size is adjusted by the flexibility or alignment of its parent. The CSS width property may also be used. </dd></dl>
<div class="noinclude">
<p>In this example, the preferred width of the inner hbox will be displayed at 40 pixels. The displayed width is also 40 pixels as no flexibility or alignment applies.</p>
<pre>&lt;hbox&gt;
  &lt;hbox width="40" style="background-color: red;"&gt;
    &lt;label value="40"/&gt;
  &lt;/hbox&gt;
&lt;/hbox&gt;
</pre>
<p>However, in the following example, despite that the preferred width of the box is 30 pixels, the displayed size of the box will be larger to accommodate the larger label.</p>
<pre>&lt;vbox width="30" align="start" style="background-color: red;"&gt;
  &lt;label value="vbox xul width 10px red"/&gt;
&lt;/vbox&gt;
</pre>
<div class="note"><strong>Note:</strong> When used on <code><a title="en/XUL/treecol" href="/en/XUL/treecol">treecol</a></code> objects, the <code>width</code> attribute can be used to allow a tree to be scrolled horizontally if the column widths add up to be wider than the containing object. <span class="script">template.Fx_minversion_inline(3)</span></div>
</div>
<div class="noinclude">
<p><span class="script"> wiki.languages( { "fr": "fr/XUL/Attributs/width", "ja": "ja/XUL/Attribute/width" } ) </span></p>
</div></body></content>